{
  "name": "tais-repository",
  "version": "1.6.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "integration-example"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "cd integration-example && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
