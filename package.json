{
  "name": "tais-repository",
  "version": "1.6.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "integration-example"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "cd integration-example && npm run dev",
    "test:hunt": "node tests/silent_errors.test.js",
    "test": "npm run test:hunt"
  },
  "devDependencies": {
    "fast-check": "^4.6.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.992.0",
    "@aws-sdk/s3-request-presigner": "^3.992.0"
  }
}
